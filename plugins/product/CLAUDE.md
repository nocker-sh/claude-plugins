# product プラグインの設計思想

このファイルはプラグインを編集するとき常に前提とする設計思想。エージェント定義を変更する前に必ず読む。モデル ID・skills・permission などの具体的な設定値は各エージェントの frontmatter が正本で、ここには書かない。

## 中心的なアイデア

メインセッションのエージェントは 3 種類あり、判断の所在・実装の担い方・運用の重さで分かれる。

- orchestrator: 最も判断力の高いモデルを主役に置き、設計判断・計画・受け入れ判定を自分で下す。コードを書く作業は全て worker に委託する。advisor に依存しない。
- executor: 安くて速いモデルで、基本的に自分の手で実装する。難しい実装（影響範囲が広い・全体把握が要る・コンテキストを大きく消費する）だけを worker に委託する。設計判断・計画・受け入れ判定は advisor に外出しする。
- solo: executor の軽量版。個人開発向けで、GitHub / Sentry / Issue / PR / worktree の運用を敷かず、ローカルのブランチとコミットで完結する。ドキュメントも管理台帳を持たない軽量運用。判断の向きは executor と同じ（advisor に外出し）。

共通の原則は 2 つ。メインセッションのコンテキストには判断とレビューに必要な情報だけを入れること、自分でなくてもできる仕事は責務に合ったサブエージェントに流すこと。

## モデル選定の方針

frontmatter の値を変えるときはこの方針に沿っているかを確認する。

- executor / solo は「賢さ」より「速さと安さ」で選ぶ。判断を advisor に外出ししている以上、強いモデルにする理由はない。実装力は必要なので、実装のできる中で軽いモデルを選ぶ。
- advisor は使える中で最も判断力の高いモデルにする（既定は Fable、使えないときは Opus に切り替え）。判断力の欠如を補う唯一の場所なので、ここをケチらない。
- orchestrator は判断の主役なので、使える中で最も判断力の高いモデルにする。
- worker は実装力とコンテキストの大きさで選ぶ。大きな差分やコードベース全体の把握が要る作業を一括で受けるため、長コンテキスト版を使う。
- loop は巡回と起票だけの定型作業なので軽いモデルでよい。

## 起動形態

orchestrator / executor / solo は `claude --agent product:{name}` で起動され、メインプロセスになる。サブエージェントではない。どのエージェントで起動するかがそのまま運用の選択になる（チーム開発なら orchestrator か executor、個人開発なら solo）。loop はループから直接起動される独立ワーカーで、メインセッションからは呼ばない。

## 開発フロー

orchestrator / executor の開発は Worktree・Issue・プルリクを使う。Issue に取り組むときは必ず `.worktrees/` 配下に worktree を切り、worktree の add / remove はメインプロセスだけが行う。Issue 起票・push・PR 作成など gh と git の外向き操作もメインプロセスが行う。依頼の振り分け・Issue / PR の書式・実装フェーズの手順は dev スキル（references/request.md, references/tools/）が正本。

solo は Issue / PR / worktree を使わず、現在のブランチとコミットで完結する。案件の記録先は `.docs/notes/`。

worker は実装専任で、worktree / ブランチ / Issue / PR / gh には一切関わらず、コミットで止まる。呼び出し元がどのエージェントでも変わらない。

## 変更時の禁止事項

- executor / solo に設計判断・計画・受け入れ判定を自分で下させる記述を足さない。判断は advisor という向きを崩さない（orchestrator は判断を自分で下すのが正で、この制約は適用しない）。
- orchestrator に実装を抱え込ませる記述を足さない。実装は worker という向きを崩さない（executor / solo は自分で実装するのが正で、この制約は適用しない）。
- executor / solo に advisor が使えないまま単独で判断を進める逃げ道を作らない。モデル切替を提案して止まるのが正。
- solo に GitHub / Sentry / Issue / PR / worktree の運用・判断を足さない。それが必要なら executor を使うのが正。
- メインセッションのコンテキストを重くする方向の変更（大出力の取り込み、全ファイル読みの要求）を足さない。
- サブエージェント → サブエージェントの委譲を導入しない。委譲はメインプロセス → サブエージェントの一方向のみ。
